name: CI

on:
  push:
  pull_request:
    branches:
      - dev

concurrency:
  group: ${{github.workflow}}-${{github.ref}}
  cancel-in-progress: true

jobs:
  foundry:
    uses: BreadchainCoop/etherform/.github/workflows/_foundry-cicd.yml@Rubydusa/saving-circles-with-upgrade-safety-no-baseline
    permissions:
      contents: read
      pull-requests: write
    with:
      main-branch: "dev"
      package-manager: "yarn"
      deploy-on-pr: true
      deploy-script: "script/Deploy.sol:Deploy"
      run-slither: false
      run-upgrade-safety: true
      run-coverage: true
      run-halmos: true
    secrets:
      PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
      RPC_URL: ${{ secrets.RPC_URL }}
      DEPLOY_ENV_VARS: ${{ secrets.DEPLOY_ENV_VARS }}

  lint:
    name: Lint Commit Messages
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: wagoid/commitlint-github-action@v5

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: nightly

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: "yarn"

      - name: Install dependencies
        run: yarn --frozen-lockfile --network-concurrency 1

      - run: yarn lint:check
